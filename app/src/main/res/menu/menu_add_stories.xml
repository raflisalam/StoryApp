<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:menuCategory="container">
        <item android:id="@+id/button_add"
            android:title="add stories"
            android:icon="@drawable/ic_add_black"
            app:showAsAction="always" />
        <item android:id="@+id/button_settings"
            android:title="settings"
            android:icon="@drawable/ic_settings"
            app:showAsAction="always" />
        <item android:id="@+id/button_logout"
            android:title="@string/btn_logout"
            android:icon="@drawable/ic_logout"
            app:showAsAction="ifRoom"/>
    </group>

</menu>